resources:
  pipelines:
    pipeline1:
      name: Pipeline1
      libraries:
        - glob:
            include: ../src/transformations/**
      catalog: databricksetienne
      channel: CURRENT
      continuous: false
      photon: true
      root_path: ../src
      schema: bronze
      serverless: true
